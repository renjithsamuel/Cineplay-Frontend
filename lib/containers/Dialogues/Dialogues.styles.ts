import { themeValues } from "@/cineplay/lib/constants/ThemeConstants";
import { makeStyles } from "@mui/styles";

export const useDialoguesStyles = makeStyles((theme) => ({
  dialoguesRoot: {
    backgroundColor: "black",
  },
}));
